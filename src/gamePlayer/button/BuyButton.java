package gamePlayer.button;

import javafx.event.ActionEvent;
import javafx.event.EventHandler;

public class BuyButton extends AButton{

	public BuyButton (EventHandler<ActionEvent> event) {
		super("Buy", event);
	}
}